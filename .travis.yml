language: node_js
node_js:
  - "stable"
services:
  - "postgres"
before_script:
  - npm install sequelize-cli -g
  - psql -c "CREATE DATABASE pms_test;" -U postgres
  - sequelize db:migrate --env test
after_success:
  - npm run test:cover
